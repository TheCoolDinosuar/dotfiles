# ls
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# tmux aliases
alias t="tmux"
alias tn="t new -s"
alias ta="t a -t"

alias mv="mv -i" # Prompts overwrite of existing dir
alias mkdir="mkdir -p" # Creates directories if needed
alias vim="nvim"
